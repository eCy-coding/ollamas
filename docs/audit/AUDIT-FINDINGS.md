# ollamas — Proje-Geneli "Çalışmayan Fonksiyon" Denetimi (Faz 11)

> ## ⚠️ RE-VERIFICATION 2026-06-29 (vFinal) — bu snapshot BAYAT
> Aşağıdaki Faz-11 tablosu **2026-06-25 anlık görüntüsü**; çoğu satır o tarihten beri **ZATEN DÜZELTİLDİ** (claims withLock, ai.ts localhost, ReAct tool_calls history, providers streaming, mcp resources, bridge-guard realpath, content-pipeline/diagnostic try-catch, vb.). **Fix etmeden önce CANLI kodu doğrula** — kör-fix çalışan kodu bozar.
>
> **vFinal adversarial bug-hunt** (43-agent Workflow: 4 finder → 3-skeptic refute, ≥2/3 = CONFIRMED) → **5 yeni gerçek bug bulundu + DÜZELTİLDİ** (a970133/65f6abc), 8 false-positive reddedildi:
> | id | site | sev | fix |
> |---|---|---|---|
> | consumeAuthCode | server/store/index.ts | CRIT-sec | DELETE-changes arbiter (concurrent OAuth code replay) |
> | rotateRefreshToken | server/store/index.ts | HIGH-sec | conditional UPDATE used=0 (RFC 9700 reuse detect) |
> | recordInvoice | server/store/index.ts | CRIT-bill | atomic insert-if-absent (duplicate invoice) |
> | /api/keys/test | server.ts:418 | MED | missing provider → 400 (was corrupt store) |
> | /api/workspace/file | server.ts:1124 | HIGH | missing relativePath → 400 (was path.join 500) |
> | webhook reclaimStale | server/store+webhooks | — | runtime requeue of crash-stranded deliveries |
>
> Reddedilen (RE-FIX ETME): server.ts:1224/2008, MultiAgentPipeline:88, ReactAgentTab:214, GoogleDriveBrowser:30, SaaSAdmin:60, store:296/206. Canlı doğrulama: selftest 0-FAIL · CLS 0.000/SAĞLIKLI · İstemci Hataları 0 · 46/46 store/oauth/webhook test green · tsc 0.

_Tarih: 2026-06-25 · branch chore/p1-hardening · READ-ONLY audit (kod değişmedi)_

## Özet
- **Kapsam:** 392 kaynak dosya · 39906 LOC · 66 dispatch ünitesi · 13 grup. **Kapsama: %100** (partition union == tüm dosyalar, kör nokta yok).
- **Denetçi seçimi (AMPİRİK benchmark):** kazanan = **qwen3-coder:480b-cloud** (qwen3-coder:480b-cloud=1, qwen3:8b=1, gemini=0). Fixture: planted-bug recall/precision; gemini timeout=0, qwen3:8b co-perfect ama RAM-bound.
- **Metodoloji (3 katman, hepsi Tier-1 doğrulamalı):** (1) ollamas fleet 66/66 ünite (model=qwen3-coder:480b-cloud) — coverage + model-attribution; (2) Tier-1 deterministik (canlı endpoint smoke + kök-kod okuma); (3) Tier-1 derin (3 Claude general-purpose sub-agent runtime cluster'larda). **Realist/tarafsız:** yalnız reproduce/kod-definitif = CONFIRMED.
- **Bulgular:** 32 CONFIRMED (CRITICAL:3 · high:11 · medium:11 · low:7) · 35 SUSPECTED (CRITICAL:0 · high:13 · medium:10 · low:12) · 2 REJECTED (Tier-1 çürüttü, false-positive).
- **✅ DÜZELTİLEN (Faz11B-13, 9):** T1-001 `server/ai.ts:21` · D-001 `server/mcp/server.ts:169` · D-002 `server/tool-registry.ts:330` · D-003 `server.ts:727` · O-001 `orchestration/bin/lib/bench.ts:119` · O-002 `orchestration/bin/lib/claims.ts:116` · P2-001 `orchestration/bin/heartbeat.ts:22` · P2-004 `bin/host-bridge/lib/bridge-guard.mjs:24` · P2-008 `orchestration/bin/lib/suppress.ts:26` — kök-fix + TDD + gate, merged main.

## Kapsama Tablosu (kör nokta yok — her dosya tek sahip)

| Grup | Ünite | Dosya | LOC | Denetçi | Fleet tamam | Gerçekten-okudu | Fleet aday-bulgu |
|---|---|---|---|---|---|---|---|
| A1-core | 2 | 2 | 3167 | qwen3-coder:480b-cloud | 2/2 | 0/2 | 0 |
| A10-frontend | 7 | 41 | 5460 | qwen3-coder:480b-cloud | 7/7 | 0/7 | 0 |
| A11-scripts | 7 | 44 | 3182 | qwen3-coder:480b-cloud | 7/7 | 0/7 | 0 |
| A12-bridges | 7 | 43 | 2717 | qwen3-coder:480b-cloud | 7/7 | 7/7 | 0 |
| A13-misc | 1 | 2 | 193 | qwen3-coder:480b-cloud | 1/1 | 1/1 | 0 |
| A2-tools | 1 | 3 | 1083 | qwen3-coder:480b-cloud | 1/1 | 0/1 | 0 |
| A3-mcp | 2 | 8 | 1084 | qwen3-coder:480b-cloud | 2/2 | 0/2 | 0 |
| A4-store | 2 | 4 | 1105 | qwen3-coder:480b-cloud | 2/2 | 2/2 | 0 |
| A5-server-rest | 3 | 21 | 2139 | qwen3-coder:480b-cloud | 3/3 | 0/3 | 0 |
| A6-orch-agents | 14 | 94 | 9683 | qwen3-coder:480b-cloud | 14/14 | 4/14 | 0 |
| A7-orch-bench | 1 | 4 | 603 | qwen3-coder:480b-cloud | 1/1 | 0/1 | 0 |
| A8-orch-fuse | 1 | 5 | 626 | qwen3-coder:480b-cloud | 1/1 | 0/1 | 0 |
| A9-tests | 18 | 121 | 8864 | qwen3-coder:480b-cloud | 18/18 | 2/18 | 0 |

> Not: fleet ollamas auditor'ı olgun kodda konservatif (benchmark recall=1.0 ama gerçek-kod aday-bulgu düşük). Gerçek bug'lar ağırlıkla Tier-1 derin+deterministik katmandan; fleet katmanı kapsama+model-attribution sağlar. `ranTerminal=false` üniteler = agent dosyaları okumadan döndü (düşük-güven; Tier-1 deterministik+derin o alanları zaten kapsıyor).

## CONFIRMED Bulgular (reproduce/kod-definitif — severity sıralı)

| model | hata (file:line) | severity | symptom | çözüm yöntemi | kanıt |
|---|---|---|---|---|---|
| claude/gp + Tier-1 verified | `orchestration/bin/lib/bench.ts:119` (rankEfficient disqualifies valid champions (rounded correctRatio gate)) | CRITICAL | aggregate() stores correctRatio = round(corr/n) where round=Math.round(n*10)/10 (1 decimal, bench.ts:138). rankEfficient gates `if (a.correctRatio <= 0.9) continue` (119). So a model 85-94.9% correct rounds to 0.9 and is EXCLUDED as champion; only >=95% (rounds to 1.0) survive. The 'most efficient correct model' selection — bench's core purpose, feeding benchprompt champion + MODEL_SELECTION — silently drops strong models or picks a slower one / none. | Rank on the RAW unrounded ratio (keep a correctRatioRaw), or change the gate to `< 0.9` AND don't round the value used for gating. | Code bench.ts:109 round(corr/rs.length); :138 round=Math.round(n*10)/10; :119 <=0.9 gate. 11/12=0.9166->0.9->excluded. |
| claude/gp + Tier-1 verified | `orchestration/bin/lib/claims.ts:116` (withLock runs fn() even when lock NOT acquired) | CRITICAL | withLock retries mkdir-lock 50x; if still not held (another writer holds a non-stale lock), it falls through to `try { return fn() } finally { if (held) rmdir }` (135-139) — fn() runs UNCONDITIONALLY, so the guarded write (claim append) executes without the lock -> concurrent duplicate claims for the same lane\|version (exactly the ERR-ORCH-013 the lock exists to prevent). | After the loop: `if (!held) throw new Error('claim lock not acquired');` before calling fn(). | Code claims.ts:116-139 — held stays false after 50 fails yet fn() is called at :136. |
| claude/gp orch-bin + agent-reproduced | `orchestration/bin/heartbeat.ts:22` (wrong claim-ledger path → collision-avoidance dead) | CRITICAL | CLAIMS='orchestration/claims.jsonl' but the real ledger is seyir/work-claim.jsonl (lib/claims defaultStore). readActiveClaims() always reads [] → heartbeat re-picks work other tabs already claimed (collision-safe action pick is a permanent silent no-op). | const CLAIMS = defaultStore(seyirDir).ledgerPath (seyir/work-claim.jsonl). | Agent: seyir/work-claim.jsonl exists (3.6k), orchestration/claims.jsonl does NOT. |
| Tier-1 (deterministic smoke + code read) | `server/ai.ts:21` (ollamaHost / listModels / resolveLocalCoder / resolveDefaultModel / aiGenerate) | high | ollamaHost() returns process.env.OLLAMA_HOST (set to the docker value host.docker.internal:11434 in .env). On any local/non-docker boot that host is unreachable, so listModels() fetch(`${ollamaHost()}/api/tags`) (ai.ts:42) throws 'fetch failed' -> GET /api/ai/models returns 500, and resolveLocalCoder/resolveDefaultModel/aiGenerate (the whole colab-mirror /api/ai/* surface) break. server/providers.ts:409-411 already has the [OLLAMA_HOST, localhost, 127.0.0.1] reachability fallback; ai.ts does NOT mirror it. | Extract a shared resolveOllamaBase() that tries [process.env.OLLAMA_HOST, http://127.0.0.1:11434, http://localhost:11434] and caches the first reachable base (same pattern as providers.ts:409-411 and Faz10 host-bridge bridgeFetch). Use it in ai.ts listModels/generate/stream. Same .env then works in docker AND local. | GET http://127.0.0.1:8099/api/ai/models -> 500 {"error":"fetch failed"} (live, reproduced). Code: ai.ts:21 ollamaHost def; ai.ts:42 unguarded fetch; providers.ts:409-411 the fallback it should mirror. |
| claude/gp + Tier-1 verified | `server/mcp/server.ts:169` (resources/list (ListResourcesRequestSchema)) | high | getTree() returns {tree: FileItem[]} (array of objects) but handler does String(tree.tree\|\|'').split('\n') -> 'String([{..},{..}])' = '[object Object],[object Object]'. Every MCP resources/list returns ONE bogus resource uri 'file://[object Object],[object Object]' instead of the workspace file list. | Iterate the FileItem[] tree, flatten non-dir nodes to their relativePath, build file:// uris from those. | Live GET /api/workspace/tree returns tree as an array of objects [{name,relativePath,isDirectory,...}] -> String(array) = [object Object],...; code mcp/server.ts:169. |
| claude/gp + Tier-1 reproduced | `server/tool-registry.ts:330` (grep_search (+ server/terminal.ts:124)) | high | grep_search builds shell string `grep -rnI "${query}" .` and passes it to TerminalManager.execute which split(/\s+/) tokenizes and runs execFile (no shell). The literal double-quotes become part of argv tokens. Multi-word query 'export function' -> argv ['grep','-rnI','"export','function"','.'] -> grep: function": No such file or directory. Single-word 'providers' -> pattern arg '"providers"' (with literal quotes) -> 0 matches. grep_search returns wrong/empty results for EVERY query (functional regression of the exec->execFile security fix). | Pass the query as one argv element without surrounding quotes (a structured grep helper taking argv), not interpolated into a whitespace-tokenized string. | Reproduced deterministically: execFile('grep',['-rnI','"export','function"','.']) -> stderr 'grep: function": No such file or directory'; single-word pattern '"providers"' -> 0 stdout lines. |
| claude/gp + Tier-1 code-definitive | `server.ts:727` (ReAct multi-step assistant history omits tool_calls (+ providers.ts:684 openrouter, :834 anthropic verbatim pass-through)) | high | server.ts:727 pushes {role:'assistant', content: result.text} with NO tool_calls field; then :804 pushes {role:'tool', name, tool_call_id, content}. openrouter (providers.ts:684 messages: config.messages) and anthropic (:834 messages: nonSystemMessages) send this history VERBATIM. Strict OpenAI-compatible models + Anthropic reject a role:'tool' message not preceded by an assistant message carrying matching tool_calls/tool_use -> step 2+ of a tool-using ReAct returns HTTP 400. Multi-step tool ReAct is broken on anthropic + strict openrouter/openai models. SAFE on gemini (providers.ts:602 maps every non-system msg incl role:'tool' -> 'user') and ollama-local incl 480b-cloud (lenient) — the primary path, which is why it was never caught. | On the assistant turn push tool_calls alongside content; and in each cloud mapper translate role:'tool'+tool_call_id into that provider's tool-result shape (OpenAI tool message after assistant.tool_calls; Anthropic user message with tool_result content block; Gemini already OK). | Code: server.ts:727 (no tool_calls) + :804 (role tool); providers.ts:684 & :834 verbatim. Live repro on anthropic blocked by concurrent fleet load (server saturated) — to finalize after fleet completes; code path is unambiguous. |
| claude/gp + Tier-1 verified | `server/store/index.ts:582` (claimDeliveries (sqlite) — no requeue of stranded claims) | high | sqlite path tags due rows status='claimed_<tok>' then selects them; nothing ANYWHERE ever resets status LIKE 'claimed%' back to 'pending'. If the worker crashes/restarts between claim and markDelivery, those webhook deliveries are stranded forever (never resent, never dead-lettered). The pg path (FOR UPDATE SKIP LOCKED, line 574) sets status='claimed' and shares the same orphan risk on crash. | Add a reclaim sweep: UPDATE webhook_deliveries SET status='pending' WHERE status LIKE 'claimed%' AND created_at < (now - staleWindow), run before each claim. | grep across store/index.ts + webhooks/*: only sites that touch 'claimed' are the two claim writes (574,583) + a pending COUNT (64); no requeue/reclaim exists. |
| claude/gp server + agent-reproduced | `server/diagnostic.ts:9` (hardware calibration JSON.parse always throws → dead) | high | JSON.parse(rawOutput.replace(/'/g,'"')) on Python print(dict) output — Python emits False/True/None (not JSON) and apostrophe-swap corrupts strings; catch swallows it → calibration permanently returns {profile:'Standard'}. | calibrate_hardware.py should print json.dumps(...); parse raw (no quote-swap). | Agent: python3 -c "print({'gpu_available':False})" → JSON.parse CRASH 'Unexpected token F'. |
| claude/gp bin + agent-reproduced + Tier-1 | `bin/host-bridge/lib/bridge-guard.mjs:24` (safeWritePath no realpath → symlink escapes BRIDGE_WRITE_ROOTS) | high | path.resolve collapses ../ but does NOT resolve symlinks; a symlink inside an allowed root pointing outside (<root>/sub/evil -> /etc) passes startsWith(root) → write escapes confinement. (auth-gated: caller needs bridge HMAC/token, so defense-in-depth not remote-RCE.) | fs.realpathSync the parent dir before the startsWith check. | Agent reproduced: /tmp/stproot/sub/evil/passwd (evil→/etc) → ok:true. Tier-1 read bridge-guard.mjs:24-31 (resolve+startsWith, no realpath). |
| claude/gp orch-bin + agent-reproduced | `orchestration/bin/autopilot.ts:54` (conduct detail matches table header, never the action) | high | detailFor('conduct') matches the FIRST CONDUCTOR.md line containing → — that's the summary table header '\| Lane \| Şu an \| → Sıradaki \|' — so the conduct step ALWAYS reports column labels, never the real next-action. | Anchor on the action block (/^\*\*Eylem\|🎯/), not bare →. | Agent: CONDUCTOR.md line 7 (table header) is the first → match. |
| claude/gp orch-lib + agent-reproduced | `orchestration/bin/lib/suppress.ts:26` (empty kindPattern suppresses ALL findings) | high | loadSuppress accepts a rule with kindPattern:'' (only typeof check, not non-empty); applySuppress does f.kind.includes('') → always true → one such rule silently suppresses ALL findings of that detector, defeating no-silent-cap. | add && r.kindPattern.trim().length>0 to the filter. | Agent: 'anything'.includes('')===true; loadFilter accepts kindPattern:''. |
| claude/gp server + agent-reproduced | `server/orchestrator.ts:60` (self_analyzer tool unreachable) | high | executeTool('self_analyzer') is unreachable — self_analyzer is NOT in tools.json.tools, so the if(!tool) guard (line 36) returns 'Registration not found' before name-dispatch; SystemAnalyzer.analyze() never runs via orchestrator. | Add self_analyzer registry entry, or move name-dispatch before registry lookup. | Agent verified: self_analyzer in registry === false. |
| claude/gp scripts + agent-reproduced | `scripts/content-pipeline.mjs:75` (graceful-skeleton catch crashes on bad url → no bundle) | high | the 'always produce a bundle' catch-block calls new URL(undefined) on a source with missing url → TypeError INSIDE the catch → propagates out of draft()→run() → NO bundle written (violates the file's core guarantee). | wrap hostname extraction in try, or filter valid urls first. | Agent: node -e 'new URL(undefined)' → TypeError; research():52 can return undefined url. |
| claude/gp + Tier-1 verified | `server/providers.ts:437` (ollama-local streaming branch drops tool_calls) | medium | The streaming branch (437-469) forwards only parsed.message.content and returns {text,source,modelUsed} — it never reads parsed.message.tool_calls nor runs extractTextToolCalls(fullText), unlike the non-stream branch (483-492). Any caller with stream:true on ollama-local (/api/ai/generate stream, ai.generateTextStream, /api/generate SSE) loses all tool calls -> a streaming ReAct can't call tools. The MAIN agent loop uses stream:false (server.ts:715) so it is unaffected. | In the stream loop, capture parsed.message.tool_calls when present, and run extractTextToolCalls(fullText) before the final return (mirror 483-492). | Code: 437-469 has no tool_calls reference; 483-492 (non-stream) parses tool_calls + extractTextToolCalls fallback. |
| claude/gp + Tier-1 verified | `server/tool-interceptors.ts:124` (cacheKey omits workspaceRoot) | medium | cacheKey = `${tenantId}:${tool}:sha256(args)` does NOT include ctx.workspaceRoot. Two requests, same tenant + same args (read_file/list_tree/grep_search/...) but different workspaceRoot collide -> stale content from workspace A served for workspace B. Only active when MCP_CACHE_TTL_MS>0 (default 0 = cache disabled), so latent. | Fold ctx.workspaceRoot into the cache key. | Code: tool-interceptors.ts:124-126 hashes only args; cacheTtl() default 0 (line 123). |
| claude/gp + Tier-1 verified | `server/providers.ts:189` (modelUsed undefined after fallback) | medium | providers.ts:189 `if (prov !== config.provider) resolvedConfig.model = undefined` (intentional, so each fallback provider uses its own default). But executeProvider(resolvedConfig) then returns `modelUsed: config.model` = undefined -> any response that fell back reports model: undefined (traceability/billing attribution lost exactly when the chain switched providers). | In each adapter return `modelUsed: config.model \|\| <that provider's default>` (the same default used to build the request). | Code: providers.ts:184-189 sets resolvedConfig.model=undefined on provider switch; adapters return modelUsed: config.model. |
| claude/gp + Tier-1 verified | `server/webhooks/outbound.ts:62` (processDeliveries no per-row try/catch) | medium | for (const r of rows) await deliverOne(r) (outbound.ts:62) — deliverOne not wrapped; the worker wrapper's .catch (line 71) catches the WHOLE batch, so one deliverOne throw aborts all remaining rows in that batch, leaving them claimed-and-stuck (compounds D-004). | await deliverOne(r).catch(logAndContinue) per row inside the loop. | Code: outbound.ts:62 bare await in loop; :71 setInterval(()=>processDeliveries().catch(()=>{})) catches batch-level only. |
| Tier-1 (read-only) | `src/lib/apiClient.ts:91` (request() timeout disabled when caller passes a signal) | medium | request() arms a 30s timeout via setTimeout(()=>ctrl.abort(),timeoutMs) (line 80) but the fetch uses `signal: opts.signal ?? ctrl.signal` (line 91). When a caller supplies opts.signal, fetch listens to THAT signal, not ctrl — so ctrl.abort() on timeout does nothing and the request can hang past timeoutMs (until the caller's own signal fires, if ever). | Combine: signal: opts.signal ? AbortSignal.any([opts.signal, ctrl.signal]) : ctrl.signal. | Code apiClient.ts:80 (timer->ctrl.abort) vs :91 (fetch listens opts.signal??ctrl.signal). |
| Tier-1 (read-only) | `scripts/combo-bench.mjs:69` (benchmark counts infra-failed (demo/timeout/parse-fail) runs as model-incorrect) | medium | cache[model][task] = {correct: r.blob.includes(answer), ms, demo}. The `demo` flag is captured but NEVER used in any rate/winner computation (lines 75-94). A run that fell to demo, timed out, or failed to parse yields blob without the answer -> correct:false -> counted as the MODEL being wrong, not excluded/retried. So champion/combination selection can be skewed by infra flakiness rather than model capability (same failure class observed live in the Faz11 fleet: non-reading units silently scored as clean). | Exclude or retry demo/error runs before computing rate (e.g. skip tasks where demo\|\|!blob, or re-dispatch up to N times), and surface excluded counts. | Code combo-bench.mjs:69 captures demo; :75-94 rate/correctCount/winner never reference demo or dispatch errors. |
| claude/gp server + agent-reproduced | `server/analyzer.ts:12` (tool-gap report 100% false positives) | medium | gaps filter flags ALL tools as missing because tool names (desktop_commander) never equal script basenames (calibrate_hardware.py); the gap report is pure noise. | match against tool.entryPoint/script field, not name+'.py'. | Agent reproduced: 22/22 tools flagged as gaps. |
| claude/gp orch-bin + agent-reproduced | `orchestration/bin/autopilot.ts:99` (serverUp() curl no --fail → 500 reported up) | medium | serverUp() runs curl without --fail/-f; curl exits 0 on HTTP 500/404 as long as TCP connects → reports server up while /api/health errors, letting runHeal proceed on a broken server. | add -f to curl args (or check status). | Agent: curl -s -o /dev/null returns 0 for any HTTP status without --fail. |
| claude/gp orch-lib + agent-reproduced | `orchestration/bin/lib/critic.ts:88` (auditDuplication overlap inflated (array not deduped)) | medium | b = keywords(...).filter(...) is an array NOT deduped; overlap=b.filter(k=>a.has(k)).length counts repeated words → inflated overlap → false 'duplicate' Gap when one purpose repeats a distinctive word. | wrap b in a Set like a. | Agent: a=Set{alpha,bravo}, b=[alpha,alpha] → overlap=2, ratio=1.0. |
| claude/gp bin + agent-reproduced | `bin/host-bridge/tools/kill_process.mjs:12` (default kill (no --sig) → target undefined → broken) | medium | with no --sig, sigIdx=-1 so filter i!==sigIdx+1 drops index 0 → target becomes undefined → throws 'target required'; the registry builder omits --sig by default, so default kills are broken. | drop only when sigIdx>=0 && i===sigIdx+1. | Agent reproduced: pickTarget(['1234']) → undefined. |
| claude/gp scripts + agent-reproduced | `scripts/system-monitor.mjs:68` (MCP SSE parse breaks → builtin-count invariant silently skipped) | medium | r.body.slice(r.body.indexOf('{')) + JSON.parse on a text/event-stream body (data: {...}\n\n multi-frame / trailing event:/id: lines) fails, caught silently → the builtin>=29 invariant check silently degrades to SKIP, never asserting. | strip the 'data: ' prefix and parse the first SSE frame only. | Agent code-read; slice-to-end includes trailing framing that breaks JSON.parse. |
| claude/gp + Tier-1 verified | `server/files.ts:118` (git-status overlay misclassifies staged/deleted/renamed) | low | flag=line.substring(0,2).trim() collapses the porcelain XY columns: staged-add 'A ' -> 'A' -> reported 'untracked'; deleted ('D '/' D') and renamed ('R ') -> 'D'/'R' match NO case -> status 'none' -> DROPPED (deleted/renamed files show no git badge in the workspace tree); the `flag==='Staged'` branch is dead code (git porcelain never emits 'Staged'). | Parse X (staged, col0) and Y (worktree, col1) separately and map D->deleted, R->renamed, A(col0)->staged. | Code files.ts:114-122: cases only handle ??/A/M/MM/Staged; D and R fall through to 'none'. |
| claude/gp + Tier-1 verified | `server/providers.ts:461` (streaming tokens lost when done chunk lacks eval_duration) | low | Return with tokensPerSec only fires when parsed.done && eval_count && eval_duration all truthy; a done chunk with eval_count but no eval_duration falls to line 469 returning no tokens -> billing meter under-counts. | Capture eval_count/duration from the done chunk regardless; compute tps when duration>0. | Code: providers.ts:461 conjunction confirmed. |
| claude/gp + Tier-1 verified | `server.ts:866` (firstUserMsg.content.slice/.length assumes string) | low | server.ts:866 firstUserMsg.content.slice(0,40) + .content.length with NO guard. A user message whose content is absent/non-string (multimodal or tool-shaped history replay) throws inside the session-persist block -> the just-completed ReAct session save is lost. Edge trigger (normal user content is a string). | const c = typeof firstUserMsg.content === 'string' ? firstUserMsg.content : ''; use c.slice/c.length. | Code server.ts:866-867 unguarded .slice/.length on content. |
| Tier-1 (read-only) | `orchestration/bin/lib/optimize.ts:98` (optimalConfig dead branch) | low | `if (modelVramGb(model) >= 18 && num_ctx > 8192) num_ctx = 8192` is unreachable: num_ctx max assigned above is 8192, so the big-model ctx-reduction never fires (a 30B model never gets ctx reduced). Intent not realized / dead code. | Cap big models lower (e.g. num_ctx=4096 when modelVramGb>=18), or remove the dead branch. | Code optimize.ts:92-98 — num_ctx never exceeds 8192. |
| claude/gp server + agent-reproduced | `server/orchestrator.ts:18` (jobId collision (Math.random base36)) | low | Math.random().toString(36).substring(7) jobId (~36^5) collides; a collision overwrites a live job's status entry in JOB_STORE. | crypto.randomUUID(). | Agent reproduced: 5 collisions / 100k IDs. |
| claude/gp scripts + agent-reproduced | `scripts/content-pipeline.mjs:68` (missing title → '# undefined' output) | low | draft() only validates obj.body_markdown; an LLM reply omitting title yields post.md header '# undefined' and seo.json with undefined fields. | default d.title/seo fields to topic. | Agent reproduced: '# ${({body_markdown:'x'}).title}' → '# undefined'. |
| claude/gp scripts + agent-reproduced | `scripts/substack-digest.mjs:39` (sender mask greedy → fragments counts) | low | sender masking /\+.*@/ is greedy and keys bySender on the full 'Display <addr>' string; a sender with a '+' before an unrelated '@' mangles the key, fragmenting frequency/rising counts. | anchor mask to local-part: /\+[^@]*@/. | Agent: 'a+x@b, c@d'.replace(/\+.*@/,'@') → 'a@d' (over-eats). |

## SUSPECTED Bulgular (makul, Tier-1 reproduce bekliyor)

| model | hata (file:line) | severity | symptom | çözüm yöntemi | kanıt |
|---|---|---|---|---|---|
| claude/gp | `orchestration/bin/lib/fuse.ts:91` (dedupe fingerprint misses stale-test:/redlane: prefixes) | high | fp() prefix-strip regex doesn't include the `stale-test:`/`redlane:` prefixes that qualityToReqs emits (fuse.ts:66,83) -> same-lane requirements don't merge -> duplicate/contradictory entries (e.g. gate:backend + stale-test:backend both survive) in REQUIREMENTS. | Add stale-test\|redlane to the prefix alternation in fp(). | Agent code-read fuse.ts:66/83/91. |
| claude/gp | `orchestration/bin/driftguard.ts:64` (version-source gate resolves wrong roadmap file (silent no-op)) | high | findFile (shared.ts breadth-first) matches root COLAB_ROADMAP.md for /roadmap.*\.md$/i before descending to orchestration/ROADMAP_ORCHESTRATION.md -> declaredVersion='' -> checkVersionSources emits 0 rows every run; the HARD version-drift gate never actually checks. (plan-next.ts:232 shares the same wrong-file resolution.) | Anchor the regex (e.g. prefer orchestration/ROADMAP_ORCHESTRATION.md) or pass an explicit path. | Agent code-read driftguard.ts:64 + shared.ts findFile. |
| claude/gp | `orchestration/bin/heartbeat.ts:33` (brittle relative TSX path breaks in worktree) | high | TSX resolved via ../../../ollamas/node_modules (relative) instead of ANCHOR; run from .claude/worktrees/<wt>/orchestration/bin it resolves a missing path -> every execJson throws -> runSource()=null -> heartbeat tick silently aborts. | const TSX = join(ANCHOR, 'node_modules','.bin','tsx') (as autopilot/doctor do). | Agent code-read heartbeat.ts:33; fuse.ts:29 shares the class but resolves in this checkout. |
| claude/gp | `orchestration/bin/autopilot.ts:131` (conduct RED-tier exit-1 drops next-action) | high | conduct exits 1 on a normal RED-tier gate; runStep marks ok:false and summarizeAutopilot sets next-action to '—' exactly when an action exists -> AUTOPILOT.md hides the next action on RED. | Tolerate conduct exit-1 (capture stdout) like fuse.ts:42, then read CONDUCTOR.md. | Agent code-read autopilot.ts:131. |
| claude/gp | `orchestration/bin/lib/claims.ts:180` (renewClaim no ownership/status check (LWW clobber)) | high | renewClaim appends status:'claimed',ts:now for any tab with no ownership/state guard -> tab B renewing A's lane wins foldClaims LWW -> A silently loses the claim; can also revive done/released claims. | Only renew if the active claim's tab===opts.tab and state not done/released. | Agent code-read claims.ts:180. |
| claude/gp | `scripts/firecrawl.mjs:27` (--search multi-word query truncated to first word) | high | target = args.find(non-flag) returns only the first token; call('/search',{query:target}) searches one word for a multi-word query. | Build query from args.slice(searchIdx+1).join(' '). | Agent: ['--search','ai','layoffs']->target='ai'. |
| claude/gp | `scripts/e2e_verify.ts:8` (Browser Search E2E green-lights a simulation) | high | browser_search is in tools.json but unhandled in executeTool -> falls to localExecution returning {executed:true,output:'Simulation Success'}; .success true -> the E2E asserts success on a no-op. | Route browser_search to a real impl or drop the success claim. | Agent code-read orchestrator.ts:35/63/108 + tools.json. |
| claude/gp | `src/components/SaaSAdmin.tsx:59` (addHook secret.slice on undefined) | high | addHook does r.secret.slice(0,16) unconditionally; if POST /api/saas/webhooks returns no secret (exists/validation/older server) -> TypeError -> webhook appears unsaved, error shown as 'Cannot read properties of undefined' not the real cause. | guard r.secret before slice/copy. | Agent code-read SaaSAdmin.tsx:59. |
| claude/gp | `src/components/ReactAgentTab.tsx:323` (write_file approval throws on null args) | high | On a write_file SSE step, setPendingApproval({path:parsed.args.path,...}); if args is null/absent (error/partial trace) -> throws in the SSE loop -> caught as 'Could not parse SSE', approval wizard never opens, write silently lost. | null-check parsed.args?.path before opening approval. | Agent code-read ReactAgentTab.tsx:322-323. |
| claude/gp bin + code-read | `bin/host-bridge/terminal-bridge.mjs:224` (bridge token compare not constant-time) | high | req.headers['x-bridge-token'] === TOKEN is a non-constant-time compare → timing side-channel on the bridge token (the HMAC path already uses timingSafeEqual). | crypto.timingSafeEqual over equal-length buffers. | Agent code-read terminal-bridge.mjs:224. |
| claude/gp bin + code-read | `bin/host-bridge/terminal-bridge.mjs:255` (/read HMAC verifies over empty path/body → HMAC-only clients 401) | high | /read calls authed(req) with no raw/bridgePath, so HMAC verification runs over body='' path='' and never matches a real client signature → every HMAC-only client (server with no token) gets 401 on /read; bridgeRead/health_probe term snapshot dead when only HOST_BRIDGE_HMAC_SECRET is set. | authed(req, '', '/read') with proper GET method/path canonicalization. | Agent code-read; HMAC branch signs over /read but verify uses ''. |
| claude/gp frontend + code-read | `src/components/ClusterManager.tsx:14` (consent/leave/cpu-cap handlers are dead (no backend call)) | high | handleConsent/handleLeave only set local state, never POST /api/cluster/consent or /api/cluster/leave; the CPU-cap slider never POSTs numCtxLimit to /api/cluster/config → Join/Leave Mesh + resource governor have zero backend effect (routes exist at server.ts:1913/1925/1897). | await api.post('/api/cluster/consent',{approved:true}) etc.; debounced /api/cluster/config for cpuCap. | Agent code-read ClusterManager.tsx:14-22 (no api.* call); server routes exist. |
| claude/gp frontend + code-read | `src/components/GoogleDriveBrowser.tsx:38` (OAuth token never refreshed → Drive 401 with no recovery) | high | fetchFiles effect depends only on [token]; the Google OAuth accessToken expires (~1h) and is never refreshed → every Drive GET/DELETE 401s with no recovery; user sees a generic error. | Catch 401 → trigger re-auth/refresh before the call. | Agent code-read; firebase.ts getAccessToken returns cached token, no refresh. |
| claude/gp (unverified) | `server/providers.ts:336` (custom-openai env key unresolved + db.data.keys unguarded) | medium | getEnvKeyName('custom-openai') returns '' so an env-configured custom endpoint never joins keyPool; line ~750 indexes db.data.keys['custom-openai-endpoint'] without the \|\|{} guard hasKey uses -> TypeError if db.data.keys undefined. | Add custom-openai case to getEnvKeyName; guard db.data.keys. | Agent code-read; not independently reproduced. |
| claude/gp | `orchestration/bin/lib/driftguard.ts:79` (version-source compares incompatible numbering schemes) | medium | When the roadmap DOES resolve, it compares vO/Faz numbering (roadmapCurrent 'vO16'->16) vs git tag 'v1.22.1'->1 -> false HARD drift -> exit 1 (currently masked only because O-004 makes roadmapCurrent=''). | Compare only same-scheme sources; skip git-tag when roadmap uses vO/Faz. | Agent code-read driftguard.ts:79. |
| claude/gp | `src/components/MultiAgentPipeline.tsx:189` (error break exits inner loop only; later chunks overwrite fail status) | medium | On data.error SSE, code break exits one chunk's line loop, not the stream; streamPost keeps consuming; later stage events setStages overwrite the 'fail' status back to running/done. | set an errored flag (or abort the controller) and ignore further stage updates. | Agent code-read MultiAgentPipeline.tsx:189. |
| claude/gp | `src/components/SaaSAdmin.tsx:91` (auto-load effect deps [] never refires on token paste) | medium | useEffect(()=>{if(adminToken)refresh()},[]) runs once on mount; a new operator pasting the token later never auto-loads until manual Refresh. | depend on [adminToken] or refresh on token change. | Agent code-read SaaSAdmin.tsx:91. |
| claude/gp server + code-read | `server/orchestrator.ts:104` (localExecution returns fake 'Simulation Success') | medium | localExecution never executes — builds a command string, logs it, returns output:'Simulation Success'; surrounding try/catch is dead; every non-MCP/API/named tool silently returns fake success. | invoke the binary or return an explicit not-implemented error. | Agent code-read: body synchronous, no exec/await. |
| claude/gp server + code-read | `server/analyzer.ts:6` (readFileSync cwd-relative, no guard → ENOENT crash) | medium | fs.readFileSync('tools.json') uses a cwd-relative path with no try/catch → throws ENOENT and crashes the caller when cwd != repo root. | resolve via path.join(__dirname,...) + guard. | Agent code-read; orchestrator.ts:14 uses process.cwd() for same file, analyzer doesn't. |
| claude/gp server + code-read | `server/artifacts.ts:34` (loadManifest permanently caches null) | medium | loadManifest caches null if manifest.json absent on first call (boot before make build-all); discoverBinaries then reports 0 binaries forever (no fs fallback unlike resolveBinary). | cache only successful reads, or re-stat on null. | Agent code-read: cache!==undefined short-circuits retries. |
| claude/gp server + code-read | `server/backup.ts:122` (S3 upload uses deprecated SigV2 auth → 401/403) | medium | S3 upload sets Authorization: AWS <access>:<secret> (SigV2) which real S3/MinIO rejects; non-presigned uploads 401/403 though the path is advertised as working. | use SigV4 or document presigned-URL-only. | Agent code-read; comment admits 'requires complex crypto signing'. |
| claude/gp orch-bin + code-read | `orchestration/bin/status.ts:147` (git ahead/behind swapped + mislabeled) | medium | git rev-list --left-right --count @{u}...HEAD outputs <behind>TAB<ahead>; .replace shows 'behind/ahead' but header is ↑/↓ and legend says 'ahead/behind' → values swapped/mislabeled. | swap to HEAD...@{u} or relabel column. | Agent: git left=@{u}-only=behind, right=HEAD-only=ahead. |
| claude/gp frontend + code-read | `src/components/BackupControl.tsx:82` (FileReader no onerror → restore silently frozen) | medium | FileReader has onload but no onerror; choosing a corrupt/unreadable .enc file silently does nothing — no notify, loading never toggles, restore appears frozen. | reader.onerror = () => onNotify('Failed to read backup file','error'). | Agent code-read BackupControl.tsx:82-107 (only onload). |
| claude/gp (unverified) | `server/webhooks/outbound.ts:23` (verifyWebhook split('=') truncates values containing '=') | low | header parsed via kv.split('=') taking [0]/[1]; any '=' in the value (base64 padding/future fields) truncates the signature -> false negatives. | split('=') then slice(1).join('=') or a regex. | Agent code-read; no current input triggers it. |
| claude/gp (unverified) | `server/middleware/rate-limit.ts:35` (evictIfNeeded only runs on new key) | low | evictIfNeeded gated behind !buckets.has(tenantId); an existing idle tenant set never triggers eviction, so churn of returning tenants can exceed MAX_BUCKETS. | Call evictIfNeeded unconditionally (cheap when under cap). | Agent code-read. |
| claude/gp (unverified) | `server/billing/stripe.ts:185` (subscription handler catch conflates unknown-plan vs DB error) | low | catch{} swallows both unknown-plan and transient DB errors -> a valid plan with a DB error is reported handled:false-ish; Stripe won't retry -> tenant stuck on old plan. | Distinguish unknown-plan (swallow) from DB error (rethrow -> 500 -> Stripe retries). | Agent code-read. |
| claude/gp | `server/orchestrator.ts:114` (dead logActivity/logFailure) | low | logActivity + logFailure defined but never called (real logging is inline console.log). | Delete both. | Agent grep: zero call sites. |
| claude/gp | `scripts/ready.mjs:30` (have('ollama') PATH-only -> false hint for GUI Ollama) | low | have('ollama') only scans $PATH; Ollama.app (daemon-only, no CLI on PATH) yields a false 'install ollama' hint even when the daemon is up. | Probe daemon presence, not PATH, for the hint. | Agent code-read ready.mjs:58. |
| claude/gp | `src/components/ClusterManager.tsx:24` (unguarded setState after unmount) | low | mount effect api.get('/api/cluster/capabilities') has no unmount guard; switching tabs before resolve -> setOrchestratorError after unmount (React warning / wasted set). | add mounted ref or AbortController (like useUsage/useLogbook). | Agent code-read ClusterManager.tsx:24. |
| claude/gp | `src/components/WorkspaceTree.tsx:30` (double round-trip when server root != prop) | low | fetchTree calls onPathChange(workspaceRoot) when root differs; effect dep [activePath] refires fetchTree -> a second round-trip on every mount where server root != prop. | compare and skip, or mount-only fetch. | Agent code-read WorkspaceTree.tsx:30-43. |
| claude/gp | `src/components/TelemetryCockpit.tsx:17` (dead cpuHistory state (+ setState every 5s)) | low | cpuHistory computed/set each poll but never rendered -> pure dead state + a setState every 5s. | drop the state+effect or render it. | Agent code-read; no JSX consumer. |
| claude/gp server + code-read | `server/orchestrator.ts:15` (JOB_STORE unbounded memory leak) | low | completed/failed jobs never evicted from JOB_STORE Map → long-running process leaks memory. | TTL/LRU eviction on completion. | Agent code-read: no delete/cap. |
| claude/gp server + code-read | `server/backup.ts:25` (backup salt is dead (not in key derivation)) | low | salt = randomBytes(16) prepended to the blob and re-read on restore, but NEVER fed into key derivation (key is raw masterKey); the 'zero-knowledge derive from salt' comment is false. | drop salt or actually HKDF the key from it. | Agent code-read: restore reads salt then ignores it. |
| claude/gp bin + code-read | `bin/host-bridge/tools/process_port.mjs:9` (process_port ok always true) | low | lsof ... \|\| echo 'no listener' always exits 0 → ok:r.exitCode===0 always true even when nothing listens (the listening flag is the only real signal). | derive ok from listening, not exitCode. | Agent code-read: \|\| echo masks lsof exit 1. |
| claude/gp orch-bin + code-read | `orchestration/bin/dod.ts:107` (suppress count breakdown vs total mismatch) | low | stderr breakdown uses pre-suppress raw counts (lapsesR*.length) while total ${lapses.length} is post-suppress → per-category sum can exceed the total when any lapse is suppressed (cosmetic). | compute per-rule counts from filtered lapses (like critic.ts byKind). | Agent code-read dod.ts:88 (post) vs :107 (pre). |

## REJECTED (Tier-1 reproduce ÇÜRÜTTÜ — false-positive, kaydı tutulur)

| model | iddia (file:line) | neden REJECTED (kanıt) |
|---|---|---|
| claude/gp — Tier-1 REFUTED | `scripts/system-monitor.mjs:34` (app.health CRITICAL check hits wrong path (CLAIMED) -> always FAIL) | Agent claimed GET ${APP}/health 404s because the server only mounts /api/health. FALSE: live curl http://127.0.0.1:8099/health -> 200 (the server DOES expose a bare /health alias in addition to /api/health). The monitor check works. Tier-1 reproduce: curl /health -> 200 AND curl /api/health -> 200 on live :8099. Agent assumed no bare /health; there is one. |
| claude/gp frontend + Tier-1 verified | `src/hooks/useAuth.ts:16` (restored Firebase session never clears needsAuth/token) | onAuthStateChanged currentUser branch only setUser(currentUser); never setToken or setNeedsAuth(false). A returning user with a persisted session is stuck on the 'Sign in with Google' screen forever; Google Drive never loads. Tier-1: handleLogin (useAuth.ts:41) DOES setNeedsAuth(false); firebase.ts cachedAccessToken is set ONLY at googleSignIn (Google Drive OAuth token is NOT persisted by Firebase; reload→null), so a restored session showing sign-in + one-click re-consent is BY DESIGN (a popup can't open without a user gesture → silent auto-sign-in is impossible). 'stuck forever' is false. |

## Endpoint Smoke (canlı :8099)
- 200 OK: 18 endpoint.
- HATA: /api/ai/models -> 500 fetch failed

## Re-flag EDİLMEYENLER (dedup — zaten fix/known/false-positive)
- Faz10 fix: host-bridge localhost fallback, gemini model-leak. CRITICAL-1/2/3: combination-wire, demo-fallback honesty, tool-arg repairJson. 9 bilinen-fix (dotenv, localOwnerGuard, execFile injection, migration assert, …).
- semgrep FALSE-POSITIVE: path-traversal (resolveSafePath resolve+startsWith), gcm setAuthTag, terminal.ts allowlist, host-bridge JSON-transport.
- Bilinen flaky test (regresyon değil): mcp-stdio subscribe; 5 E2E boot dosyası (yüksek-RAM concurrent).
