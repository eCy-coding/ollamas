# REQUEST_EN.md — Original request, exact English translation

> Source: Emre's Turkish prompt (2026-07-01). This is the complete, faithful English
> rendering that seeded the model-council system (see `orchestration/COUNCIL_PROMPT.md`).

## Exact translation

Translate this request of mine into its complete, exact English equivalent! Debate it as a
council with sub-agents, in a way you are certain will increase efficiency! Turn every answer
and suggestion that comes out of the debate and improves the prompt into permanent prompts!
Work in a complementary and comprehensive way, mathematically and logically! Focus on the
ollamas project! Run the sub-models and agents interactively across the entire project! Which
programming language and what kind of coding needs to be done for the project to advance? To
determine this, analyze every detail end-to-end, comprehensively! Assign every model a
responsibility area — matched to its capabilities — for analyzing ollamas, and supervise their
work! Keep this running sustainably until it reaches the most efficient, fastest, most reliable,
and most correct working principle; detect the problems, gather enough information from enough
sources, and build without interruption!

## Decomposed intent (what the request actually asks for)

1. **Exact translation** — this document.
2. **Council debate** — multiple sub-agents/models deliberate to raise efficiency.
3. **Permanent prompts** — debate outputs become durable, reusable prompts (not one-shot).
4. **Focus** — the ollamas project, comprehensively and end-to-end.
5. **Interactive fleet** — run the local models/agents across the whole project.
6. **Language/coding determination** — decide which programming language and what concrete
   coding work advances each part of the project.
7. **Capability-matched responsibility** — assign every model an area it is good at, and
   supervise (verify) its work.
8. **Sustainable convergence** — keep running until the most efficient / fastest / most
   reliable / most correct working principle is reached; detect problems; gather enough
   information from enough sources; build without interruption.

Realized by: `orchestration/COUNCIL_ROSTER.json` (6), `orchestration/bin/council.ts` +
`server/council.ts` (2,5,7), `orchestration/oracle` (7 supervision), `docs/E2E_ANALYSIS.md`
(4,6), `orchestration/COUNCIL_PROMPT.md` (3), `orchestration/bin/autopilot.ts` (8).
