{
  "ts": "2026-07-02T13:58:40Z",
  "phases": [
    {
      "order": 1,
      "stream": "mjs-migration",
      "steps": [
        {
          "gap": {
            "kind": "language-migration",
            "title": "98 .mjs files still to migrate to TypeScript",
            "evidence": "git ls-files '*.mjs' = 98; concentrated in scripts (31), bin/host-bridge/tools (19), bin/host-bridge (12)",
            "severity": "P1",
            "ownerStream": "mjs-migration",
            "justification": "TS is the primary language (type-safety, single toolchain); un-migrated .mjs escapes tsc + the shared type contracts."
          },
          "recipe": {
            "approach": "Incremental, behavior-preserving .mjs → .ts migration — never a big-bang rewrite.",
            "steps": [
              "Batch by directory, smallest/leaf first (scripts → bin/host-bridge/lib → tools).",
              "Per file: rename .mjs → .ts, add explicit param/return types, keep the runtime IDENTICAL (no logic change).",
              "Update the importers' extension + any build/register manifest.",
              "Gate each file: `tsc --noEmit` clean + the file's existing test still green before moving on."
            ],
            "verify": "tsc --noEmit 0 + full test suite green after each directory batch; zero behavior diff."
          }
        }
      ],
      "p1": 1
    },
    {
      "order": 2,
      "stream": "typescript-core",
      "steps": [
        {
          "gap": {
            "kind": "route-missing",
            "title": "Frontend calls `/api/ecysearcher` but no backend route serves it",
            "evidence": "src /api call with no matching server route",
            "severity": "P1",
            "ownerStream": "typescript-core",
            "justification": "A called-but-unimplemented endpoint is a runtime 404 — a genuine missing implementation."
          },
          "recipe": {
            "approach": "Verify the call is REAL before implementing — some are URL-concat regex artifacts.",
            "steps": [
              "Open the frontend source line: is it a genuine endpoint or a base-URL concatenation artifact?",
              "If real: implement the Express handler in server/** reusing an adjacent handler pattern, with input validation + typed body + error handling.",
              "If artifact: fix the frontend call (correct the constructed URL); no backend change.",
              "Add a test (request → expected status/shape) and register the route at the choke-point."
            ],
            "verify": "New route returns the expected status for valid + invalid input; test green; no 404 for the real call."
          }
        },
        {
          "gap": {
            "kind": "route-missing",
            "title": "Frontend calls `/api/ecysearcher/api/search/search/analytics` but no backend route serves it",
            "evidence": "src /api call with no matching server route",
            "severity": "P1",
            "ownerStream": "typescript-core",
            "justification": "A called-but-unimplemented endpoint is a runtime 404 — a genuine missing implementation."
          },
          "recipe": {
            "approach": "Verify the call is REAL before implementing — some are URL-concat regex artifacts.",
            "steps": [
              "Open the frontend source line: is it a genuine endpoint or a base-URL concatenation artifact?",
              "If real: implement the Express handler in server/** reusing an adjacent handler pattern, with input validation + typed body + error handling.",
              "If artifact: fix the frontend call (correct the constructed URL); no backend change.",
              "Add a test (request → expected status/shape) and register the route at the choke-point."
            ],
            "verify": "New route returns the expected status for valid + invalid input; test green; no 404 for the real call."
          }
        },
        {
          "gap": {
            "kind": "route-missing",
            "title": "Frontend calls `/api/ecysearcher/api/search/search` but no backend route serves it",
            "evidence": "src /api call with no matching server route",
            "severity": "P1",
            "ownerStream": "typescript-core",
            "justification": "A called-but-unimplemented endpoint is a runtime 404 — a genuine missing implementation."
          },
          "recipe": {
            "approach": "Verify the call is REAL before implementing — some are URL-concat regex artifacts.",
            "steps": [
              "Open the frontend source line: is it a genuine endpoint or a base-URL concatenation artifact?",
              "If real: implement the Express handler in server/** reusing an adjacent handler pattern, with input validation + typed body + error handling.",
              "If artifact: fix the frontend call (correct the constructed URL); no backend change.",
              "Add a test (request → expected status/shape) and register the route at the choke-point."
            ],
            "verify": "New route returns the expected status for valid + invalid input; test green; no 404 for the real call."
          }
        },
        {
          "gap": {
            "kind": "stub",
            "title": "Unfinished marker in `orchestration/bin/completion-scan.ts`",
            "evidence": "TODO/FIXME/not-implemented found in file",
            "severity": "P2",
            "ownerStream": "typescript-core",
            "justification": "An explicit TODO/stub marks incomplete logic the author flagged."
          },
          "recipe": {
            "approach": "Read the marker context first — some are grep-arg false positives (the literal word TODO in code).",
            "steps": [
              "Open the file at the marker: is it real unfinished logic or an incidental occurrence of TODO/FIXME?",
              "If real: implement the flagged logic, smallest correct change, with a test.",
              "If false positive: no action (the scanner honestly flags it as 'found in file')."
            ],
            "verify": "Marker resolved (implemented + tested) or confirmed a false positive; no dangling unfinished logic."
          }
        },
        {
          "gap": {
            "kind": "stub",
            "title": "Unfinished marker in `orchestration/bin/dod.ts`",
            "evidence": "TODO/FIXME/not-implemented found in file",
            "severity": "P2",
            "ownerStream": "typescript-core",
            "justification": "An explicit TODO/stub marks incomplete logic the author flagged."
          },
          "recipe": {
            "approach": "Read the marker context first — some are grep-arg false positives (the literal word TODO in code).",
            "steps": [
              "Open the file at the marker: is it real unfinished logic or an incidental occurrence of TODO/FIXME?",
              "If real: implement the flagged logic, smallest correct change, with a test.",
              "If false positive: no action (the scanner honestly flags it as 'found in file')."
            ],
            "verify": "Marker resolved (implemented + tested) or confirmed a false positive; no dangling unfinished logic."
          }
        },
        {
          "gap": {
            "kind": "stub",
            "title": "Unfinished marker in `orchestration/bin/lib/dod.ts`",
            "evidence": "TODO/FIXME/not-implemented found in file",
            "severity": "P2",
            "ownerStream": "typescript-core",
            "justification": "An explicit TODO/stub marks incomplete logic the author flagged."
          },
          "recipe": {
            "approach": "Read the marker context first — some are grep-arg false positives (the literal word TODO in code).",
            "steps": [
              "Open the file at the marker: is it real unfinished logic or an incidental occurrence of TODO/FIXME?",
              "If real: implement the flagged logic, smallest correct change, with a test.",
              "If false positive: no action (the scanner honestly flags it as 'found in file')."
            ],
            "verify": "Marker resolved (implemented + tested) or confirmed a false positive; no dangling unfinished logic."
          }
        },
        {
          "gap": {
            "kind": "stub",
            "title": "Unfinished marker in `orchestration/bin/lib/completion.ts`",
            "evidence": "TODO/FIXME/not-implemented found in file",
            "severity": "P2",
            "ownerStream": "typescript-core",
            "justification": "An explicit TODO/stub marks incomplete logic the author flagged."
          },
          "recipe": {
            "approach": "Read the marker context first — some are grep-arg false positives (the literal word TODO in code).",
            "steps": [
              "Open the file at the marker: is it real unfinished logic or an incidental occurrence of TODO/FIXME?",
              "If real: implement the flagged logic, smallest correct change, with a test.",
              "If false positive: no action (the scanner honestly flags it as 'found in file')."
            ],
            "verify": "Marker resolved (implemented + tested) or confirmed a false positive; no dangling unfinished logic."
          }
        },
        {
          "gap": {
            "kind": "sparse-folder",
            "title": "Folder `assets` has only 1 tracked file(s) — possibly a stub lane",
            "evidence": "git ls-files assets = 1",
            "severity": "P3",
            "ownerStream": "typescript-core",
            "justification": "A near-empty top-level folder is either an intentional placeholder or an unfinished lane — verify intent (SUSPECTED, not confirmed-missing)."
          },
          "recipe": {
            "approach": "Verify intent — a near-empty folder is a placeholder OR an unfinished lane. Never fabricate code.",
            "steps": [
              "Inspect the folder + git history: is it an intentional placeholder (assets/tokens) or an unfinished lane?",
              "If intentional: add a short README explaining its purpose so it isn't mistaken for a gap.",
              "If unfinished: scope its completion as a separate, properly-planned lane (don't inline-guess its contents)."
            ],
            "verify": "Folder either documented as intentional, or a real completion lane is scoped — no invented placeholder code."
          }
        },
        {
          "gap": {
            "kind": "sparse-folder",
            "title": "Folder `client` has only 1 tracked file(s) — possibly a stub lane",
            "evidence": "git ls-files client = 1",
            "severity": "P3",
            "ownerStream": "typescript-core",
            "justification": "A near-empty top-level folder is either an intentional placeholder or an unfinished lane — verify intent (SUSPECTED, not confirmed-missing)."
          },
          "recipe": {
            "approach": "Verify intent — a near-empty folder is a placeholder OR an unfinished lane. Never fabricate code.",
            "steps": [
              "Inspect the folder + git history: is it an intentional placeholder (assets/tokens) or an unfinished lane?",
              "If intentional: add a short README explaining its purpose so it isn't mistaken for a gap.",
              "If unfinished: scope its completion as a separate, properly-planned lane (don't inline-guess its contents)."
            ],
            "verify": "Folder either documented as intentional, or a real completion lane is scoped — no invented placeholder code."
          }
        },
        {
          "gap": {
            "kind": "sparse-folder",
            "title": "Folder `packaging` has only 1 tracked file(s) — possibly a stub lane",
            "evidence": "git ls-files packaging = 1",
            "severity": "P3",
            "ownerStream": "typescript-core",
            "justification": "A near-empty top-level folder is either an intentional placeholder or an unfinished lane — verify intent (SUSPECTED, not confirmed-missing)."
          },
          "recipe": {
            "approach": "Verify intent — a near-empty folder is a placeholder OR an unfinished lane. Never fabricate code.",
            "steps": [
              "Inspect the folder + git history: is it an intentional placeholder (assets/tokens) or an unfinished lane?",
              "If intentional: add a short README explaining its purpose so it isn't mistaken for a gap.",
              "If unfinished: scope its completion as a separate, properly-planned lane (don't inline-guess its contents)."
            ],
            "verify": "Folder either documented as intentional, or a real completion lane is scoped — no invented placeholder code."
          }
        },
        {
          "gap": {
            "kind": "sparse-folder",
            "title": "Folder `tokens-light` has only 1 tracked file(s) — possibly a stub lane",
            "evidence": "git ls-files tokens-light = 1",
            "severity": "P3",
            "ownerStream": "typescript-core",
            "justification": "A near-empty top-level folder is either an intentional placeholder or an unfinished lane — verify intent (SUSPECTED, not confirmed-missing)."
          },
          "recipe": {
            "approach": "Verify intent — a near-empty folder is a placeholder OR an unfinished lane. Never fabricate code.",
            "steps": [
              "Inspect the folder + git history: is it an intentional placeholder (assets/tokens) or an unfinished lane?",
              "If intentional: add a short README explaining its purpose so it isn't mistaken for a gap.",
              "If unfinished: scope its completion as a separate, properly-planned lane (don't inline-guess its contents)."
            ],
            "verify": "Folder either documented as intentional, or a real completion lane is scoped — no invented placeholder code."
          }
        },
        {
          "gap": {
            "kind": "sparse-folder",
            "title": "Folder `ops` has only 2 tracked file(s) — possibly a stub lane",
            "evidence": "git ls-files ops = 2",
            "severity": "P3",
            "ownerStream": "typescript-core",
            "justification": "A near-empty top-level folder is either an intentional placeholder or an unfinished lane — verify intent (SUSPECTED, not confirmed-missing)."
          },
          "recipe": {
            "approach": "Verify intent — a near-empty folder is a placeholder OR an unfinished lane. Never fabricate code.",
            "steps": [
              "Inspect the folder + git history: is it an intentional placeholder (assets/tokens) or an unfinished lane?",
              "If intentional: add a short README explaining its purpose so it isn't mistaken for a gap.",
              "If unfinished: scope its completion as a separate, properly-planned lane (don't inline-guess its contents)."
            ],
            "verify": "Folder either documented as intentional, or a real completion lane is scoped — no invented placeholder code."
          }
        },
        {
          "gap": {
            "kind": "sparse-folder",
            "title": "Folder `public` has only 2 tracked file(s) — possibly a stub lane",
            "evidence": "git ls-files public = 2",
            "severity": "P3",
            "ownerStream": "typescript-core",
            "justification": "A near-empty top-level folder is either an intentional placeholder or an unfinished lane — verify intent (SUSPECTED, not confirmed-missing)."
          },
          "recipe": {
            "approach": "Verify intent — a near-empty folder is a placeholder OR an unfinished lane. Never fabricate code.",
            "steps": [
              "Inspect the folder + git history: is it an intentional placeholder (assets/tokens) or an unfinished lane?",
              "If intentional: add a short README explaining its purpose so it isn't mistaken for a gap.",
              "If unfinished: scope its completion as a separate, properly-planned lane (don't inline-guess its contents)."
            ],
            "verify": "Folder either documented as intentional, or a real completion lane is scoped — no invented placeholder code."
          }
        },
        {
          "gap": {
            "kind": "sparse-folder",
            "title": "Folder `tokens` has only 2 tracked file(s) — possibly a stub lane",
            "evidence": "git ls-files tokens = 2",
            "severity": "P3",
            "ownerStream": "typescript-core",
            "justification": "A near-empty top-level folder is either an intentional placeholder or an unfinished lane — verify intent (SUSPECTED, not confirmed-missing)."
          },
          "recipe": {
            "approach": "Verify intent — a near-empty folder is a placeholder OR an unfinished lane. Never fabricate code.",
            "steps": [
              "Inspect the folder + git history: is it an intentional placeholder (assets/tokens) or an unfinished lane?",
              "If intentional: add a short README explaining its purpose so it isn't mistaken for a gap.",
              "If unfinished: scope its completion as a separate, properly-planned lane (don't inline-guess its contents)."
            ],
            "verify": "Folder either documented as intentional, or a real completion lane is scoped — no invented placeholder code."
          }
        },
        {
          "gap": {
            "kind": "sparse-folder",
            "title": "Folder `web` has only 4 tracked file(s) — possibly a stub lane",
            "evidence": "git ls-files web = 4",
            "severity": "P3",
            "ownerStream": "typescript-core",
            "justification": "A near-empty top-level folder is either an intentional placeholder or an unfinished lane — verify intent (SUSPECTED, not confirmed-missing)."
          },
          "recipe": {
            "approach": "Verify intent — a near-empty folder is a placeholder OR an unfinished lane. Never fabricate code.",
            "steps": [
              "Inspect the folder + git history: is it an intentional placeholder (assets/tokens) or an unfinished lane?",
              "If intentional: add a short README explaining its purpose so it isn't mistaken for a gap.",
              "If unfinished: scope its completion as a separate, properly-planned lane (don't inline-guess its contents)."
            ],
            "verify": "Folder either documented as intentional, or a real completion lane is scoped — no invented placeholder code."
          }
        },
        {
          "gap": {
            "kind": "sparse-folder",
            "title": "Folder `backend` has only 5 tracked file(s) — possibly a stub lane",
            "evidence": "git ls-files backend = 5",
            "severity": "P3",
            "ownerStream": "typescript-core",
            "justification": "A near-empty top-level folder is either an intentional placeholder or an unfinished lane — verify intent (SUSPECTED, not confirmed-missing)."
          },
          "recipe": {
            "approach": "Verify intent — a near-empty folder is a placeholder OR an unfinished lane. Never fabricate code.",
            "steps": [
              "Inspect the folder + git history: is it an intentional placeholder (assets/tokens) or an unfinished lane?",
              "If intentional: add a short README explaining its purpose so it isn't mistaken for a gap.",
              "If unfinished: scope its completion as a separate, properly-planned lane (don't inline-guess its contents)."
            ],
            "verify": "Folder either documented as intentional, or a real completion lane is scoped — no invented placeholder code."
          }
        }
      ],
      "p1": 3
    },
    {
      "order": 3,
      "stream": "errors-resilience",
      "steps": [
        {
          "gap": {
            "kind": "route-unused",
            "title": "Backend route `/api/ready` is never called by the frontend",
            "evidence": "server route with no matching src /api call",
            "severity": "P3",
            "ownerStream": "errors-resilience",
            "justification": "Dead route = maintenance cost + attack surface; confirm intentional (public API?) or remove."
          },
          "recipe": {
            "approach": "Confirm intent before touching — a dead-looking route may be a public/webhook/CLI API.",
            "steps": [
              "Grep for EXTERNAL callers (docs, tests, mcp clients, curl recipes, CI) — not just the frontend.",
              "If it's a public/webhook/CLI API: KEEP it and document it (OpenAPI / README note).",
              "If genuinely dead (no consumer anywhere): remove the route + its handler + tests."
            ],
            "verify": "Either documented as intentional, or removed with the suite still green; never removed without confirming no consumer."
          }
        },
        {
          "gap": {
            "kind": "route-unused",
            "title": "Backend route `/api/openapi.json` is never called by the frontend",
            "evidence": "server route with no matching src /api call",
            "severity": "P3",
            "ownerStream": "errors-resilience",
            "justification": "Dead route = maintenance cost + attack surface; confirm intentional (public API?) or remove."
          },
          "recipe": {
            "approach": "Confirm intent before touching — a dead-looking route may be a public/webhook/CLI API.",
            "steps": [
              "Grep for EXTERNAL callers (docs, tests, mcp clients, curl recipes, CI) — not just the frontend.",
              "If it's a public/webhook/CLI API: KEEP it and document it (OpenAPI / README note).",
              "If genuinely dead (no consumer anywhere): remove the route + its handler + tests."
            ],
            "verify": "Either documented as intentional, or removed with the suite still green; never removed without confirming no consumer."
          }
        },
        {
          "gap": {
            "kind": "route-unused",
            "title": "Backend route `/api/revenue/check` is never called by the frontend",
            "evidence": "server route with no matching src /api call",
            "severity": "P3",
            "ownerStream": "errors-resilience",
            "justification": "Dead route = maintenance cost + attack surface; confirm intentional (public API?) or remove."
          },
          "recipe": {
            "approach": "Confirm intent before touching — a dead-looking route may be a public/webhook/CLI API.",
            "steps": [
              "Grep for EXTERNAL callers (docs, tests, mcp clients, curl recipes, CI) — not just the frontend.",
              "If it's a public/webhook/CLI API: KEEP it and document it (OpenAPI / README note).",
              "If genuinely dead (no consumer anywhere): remove the route + its handler + tests."
            ],
            "verify": "Either documented as intentional, or removed with the suite still green; never removed without confirming no consumer."
          }
        },
        {
          "gap": {
            "kind": "route-unused",
            "title": "Backend route `/api/github/webhook` is never called by the frontend",
            "evidence": "server route with no matching src /api call",
            "severity": "P3",
            "ownerStream": "errors-resilience",
            "justification": "Dead route = maintenance cost + attack surface; confirm intentional (public API?) or remove."
          },
          "recipe": {
            "approach": "Confirm intent before touching — a dead-looking route may be a public/webhook/CLI API.",
            "steps": [
              "Grep for EXTERNAL callers (docs, tests, mcp clients, curl recipes, CI) — not just the frontend.",
              "If it's a public/webhook/CLI API: KEEP it and document it (OpenAPI / README note).",
              "If genuinely dead (no consumer anywhere): remove the route + its handler + tests."
            ],
            "verify": "Either documented as intentional, or removed with the suite still green; never removed without confirming no consumer."
          }
        },
        {
          "gap": {
            "kind": "route-unused",
            "title": "Backend route `/api/notify/config` is never called by the frontend",
            "evidence": "server route with no matching src /api call",
            "severity": "P3",
            "ownerStream": "errors-resilience",
            "justification": "Dead route = maintenance cost + attack surface; confirm intentional (public API?) or remove."
          },
          "recipe": {
            "approach": "Confirm intent before touching — a dead-looking route may be a public/webhook/CLI API.",
            "steps": [
              "Grep for EXTERNAL callers (docs, tests, mcp clients, curl recipes, CI) — not just the frontend.",
              "If it's a public/webhook/CLI API: KEEP it and document it (OpenAPI / README note).",
              "If genuinely dead (no consumer anywhere): remove the route + its handler + tests."
            ],
            "verify": "Either documented as intentional, or removed with the suite still green; never removed without confirming no consumer."
          }
        },
        {
          "gap": {
            "kind": "route-unused",
            "title": "Backend route `/api/notify/test` is never called by the frontend",
            "evidence": "server route with no matching src /api call",
            "severity": "P3",
            "ownerStream": "errors-resilience",
            "justification": "Dead route = maintenance cost + attack surface; confirm intentional (public API?) or remove."
          },
          "recipe": {
            "approach": "Confirm intent before touching — a dead-looking route may be a public/webhook/CLI API.",
            "steps": [
              "Grep for EXTERNAL callers (docs, tests, mcp clients, curl recipes, CI) — not just the frontend.",
              "If it's a public/webhook/CLI API: KEEP it and document it (OpenAPI / README note).",
              "If genuinely dead (no consumer anywhere): remove the route + its handler + tests."
            ],
            "verify": "Either documented as intentional, or removed with the suite still green; never removed without confirming no consumer."
          }
        },
        {
          "gap": {
            "kind": "route-unused",
            "title": "Backend route `/api/generate` is never called by the frontend",
            "evidence": "server route with no matching src /api call",
            "severity": "P3",
            "ownerStream": "errors-resilience",
            "justification": "Dead route = maintenance cost + attack surface; confirm intentional (public API?) or remove."
          },
          "recipe": {
            "approach": "Confirm intent before touching — a dead-looking route may be a public/webhook/CLI API.",
            "steps": [
              "Grep for EXTERNAL callers (docs, tests, mcp clients, curl recipes, CI) — not just the frontend.",
              "If it's a public/webhook/CLI API: KEEP it and document it (OpenAPI / README note).",
              "If genuinely dead (no consumer anywhere): remove the route + its handler + tests."
            ],
            "verify": "Either documented as intentional, or removed with the suite still green; never removed without confirming no consumer."
          }
        },
        {
          "gap": {
            "kind": "route-unused",
            "title": "Backend route `/api/agent/sessions/*/events` is never called by the frontend",
            "evidence": "server route with no matching src /api call",
            "severity": "P3",
            "ownerStream": "errors-resilience",
            "justification": "Dead route = maintenance cost + attack surface; confirm intentional (public API?) or remove."
          },
          "recipe": {
            "approach": "Confirm intent before touching — a dead-looking route may be a public/webhook/CLI API.",
            "steps": [
              "Grep for EXTERNAL callers (docs, tests, mcp clients, curl recipes, CI) — not just the frontend.",
              "If it's a public/webhook/CLI API: KEEP it and document it (OpenAPI / README note).",
              "If genuinely dead (no consumer anywhere): remove the route + its handler + tests."
            ],
            "verify": "Either documented as intentional, or removed with the suite still green; never removed without confirming no consumer."
          }
        },
        {
          "gap": {
            "kind": "route-unused",
            "title": "Backend route `/api/macos-terminal` is never called by the frontend",
            "evidence": "server route with no matching src /api call",
            "severity": "P3",
            "ownerStream": "errors-resilience",
            "justification": "Dead route = maintenance cost + attack surface; confirm intentional (public API?) or remove."
          },
          "recipe": {
            "approach": "Confirm intent before touching — a dead-looking route may be a public/webhook/CLI API.",
            "steps": [
              "Grep for EXTERNAL callers (docs, tests, mcp clients, curl recipes, CI) — not just the frontend.",
              "If it's a public/webhook/CLI API: KEEP it and document it (OpenAPI / README note).",
              "If genuinely dead (no consumer anywhere): remove the route + its handler + tests."
            ],
            "verify": "Either documented as intentional, or removed with the suite still green; never removed without confirming no consumer."
          }
        },
        {
          "gap": {
            "kind": "route-unused",
            "title": "Backend route `/api/cluster/status` is never called by the frontend",
            "evidence": "server route with no matching src /api call",
            "severity": "P3",
            "ownerStream": "errors-resilience",
            "justification": "Dead route = maintenance cost + attack surface; confirm intentional (public API?) or remove."
          },
          "recipe": {
            "approach": "Confirm intent before touching — a dead-looking route may be a public/webhook/CLI API.",
            "steps": [
              "Grep for EXTERNAL callers (docs, tests, mcp clients, curl recipes, CI) — not just the frontend.",
              "If it's a public/webhook/CLI API: KEEP it and document it (OpenAPI / README note).",
              "If genuinely dead (no consumer anywhere): remove the route + its handler + tests."
            ],
            "verify": "Either documented as intentional, or removed with the suite still green; never removed without confirming no consumer."
          }
        },
        {
          "gap": {
            "kind": "route-unused",
            "title": "Backend route `/api/saas/upstreams/*` is never called by the frontend",
            "evidence": "server route with no matching src /api call",
            "severity": "P3",
            "ownerStream": "errors-resilience",
            "justification": "Dead route = maintenance cost + attack surface; confirm intentional (public API?) or remove."
          },
          "recipe": {
            "approach": "Confirm intent before touching — a dead-looking route may be a public/webhook/CLI API.",
            "steps": [
              "Grep for EXTERNAL callers (docs, tests, mcp clients, curl recipes, CI) — not just the frontend.",
              "If it's a public/webhook/CLI API: KEEP it and document it (OpenAPI / README note).",
              "If genuinely dead (no consumer anywhere): remove the route + its handler + tests."
            ],
            "verify": "Either documented as intentional, or removed with the suite still green; never removed without confirming no consumer."
          }
        },
        {
          "gap": {
            "kind": "route-unused",
            "title": "Backend route `/api/saas/upstreams/status` is never called by the frontend",
            "evidence": "server route with no matching src /api call",
            "severity": "P3",
            "ownerStream": "errors-resilience",
            "justification": "Dead route = maintenance cost + attack surface; confirm intentional (public API?) or remove."
          },
          "recipe": {
            "approach": "Confirm intent before touching — a dead-looking route may be a public/webhook/CLI API.",
            "steps": [
              "Grep for EXTERNAL callers (docs, tests, mcp clients, curl recipes, CI) — not just the frontend.",
              "If it's a public/webhook/CLI API: KEEP it and document it (OpenAPI / README note).",
              "If genuinely dead (no consumer anywhere): remove the route + its handler + tests."
            ],
            "verify": "Either documented as intentional, or removed with the suite still green; never removed without confirming no consumer."
          }
        },
        {
          "gap": {
            "kind": "route-unused",
            "title": "Backend route `/api/saas/self/keys` is never called by the frontend",
            "evidence": "server route with no matching src /api call",
            "severity": "P3",
            "ownerStream": "errors-resilience",
            "justification": "Dead route = maintenance cost + attack surface; confirm intentional (public API?) or remove."
          },
          "recipe": {
            "approach": "Confirm intent before touching — a dead-looking route may be a public/webhook/CLI API.",
            "steps": [
              "Grep for EXTERNAL callers (docs, tests, mcp clients, curl recipes, CI) — not just the frontend.",
              "If it's a public/webhook/CLI API: KEEP it and document it (OpenAPI / README note).",
              "If genuinely dead (no consumer anywhere): remove the route + its handler + tests."
            ],
            "verify": "Either documented as intentional, or removed with the suite still green; never removed without confirming no consumer."
          }
        },
        {
          "gap": {
            "kind": "route-unused",
            "title": "Backend route `/api/saas/self/keys/*/revoke` is never called by the frontend",
            "evidence": "server route with no matching src /api call",
            "severity": "P3",
            "ownerStream": "errors-resilience",
            "justification": "Dead route = maintenance cost + attack surface; confirm intentional (public API?) or remove."
          },
          "recipe": {
            "approach": "Confirm intent before touching — a dead-looking route may be a public/webhook/CLI API.",
            "steps": [
              "Grep for EXTERNAL callers (docs, tests, mcp clients, curl recipes, CI) — not just the frontend.",
              "If it's a public/webhook/CLI API: KEEP it and document it (OpenAPI / README note).",
              "If genuinely dead (no consumer anywhere): remove the route + its handler + tests."
            ],
            "verify": "Either documented as intentional, or removed with the suite still green; never removed without confirming no consumer."
          }
        },
        {
          "gap": {
            "kind": "route-unused",
            "title": "Backend route `/api/saas/usage` is never called by the frontend",
            "evidence": "server route with no matching src /api call",
            "severity": "P3",
            "ownerStream": "errors-resilience",
            "justification": "Dead route = maintenance cost + attack surface; confirm intentional (public API?) or remove."
          },
          "recipe": {
            "approach": "Confirm intent before touching — a dead-looking route may be a public/webhook/CLI API.",
            "steps": [
              "Grep for EXTERNAL callers (docs, tests, mcp clients, curl recipes, CI) — not just the frontend.",
              "If it's a public/webhook/CLI API: KEEP it and document it (OpenAPI / README note).",
              "If genuinely dead (no consumer anywhere): remove the route + its handler + tests."
            ],
            "verify": "Either documented as intentional, or removed with the suite still green; never removed without confirming no consumer."
          }
        }
      ],
      "p1": 0
    }
  ]
}
