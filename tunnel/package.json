{
  "name": "ollamas-tunnel",
  "version": "7.0.0",
  "private": true,
  "type": "module",
  "description": "ollamas tunnel/switch lane — sovereign zero-account MacBook<->iOS e2e reachability",
  "scripts": {
    "test": "node --test",
    "typecheck": "tsc --noEmit",
    "tunnel": "node src/cli.ts",
    "whoami": "bash scripts/whoami.sh"
  },
  "engines": {
    "node": ">=24"
  }
}
